import React from 'react'

const MoreButton = (props) => {
    return <button onClick={/* Fill me in! */ (event) => props.addSushis(event)}>
            More sushi!
          </button>
}

export default MoreButton